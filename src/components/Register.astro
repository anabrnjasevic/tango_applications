---
import RegistrationForm from './RegistrationForm.astro';
import { copy } from '../data/site';
import { getRegistrationState } from '../lib/registration-schedule';

const registration = getRegistrationState();
---

<section id="register" class="section-pad bg-surface-elevated">
  <div class="mx-auto max-w-3xl">
    <div class="mb-10 text-center" data-reveal="fade">
      <h2 class="font-display text-4xl text-white md:text-6xl">{copy.registerHeadline}</h2>
      <p class="mt-4 text-lg text-white/70">
        {
          registration.isRegistrationOpen
            ? copy.registerIntro
            : registration.isBeforeOpen
              ? registration.registrationNotOpenMessage
              : registration.registrationClosedMessage
        }
      </p>
    </div>

    <div data-reveal="scale" class="card-dark">
      <RegistrationForm />
    </div>
  </div>
</section>
